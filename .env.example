DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pdf_viewer
BETTER_AUTH_SECRET=       # openssl rand -base64 32
BETTER_AUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_GENERATIVE_AI_API_KEY=   # Gemini API key (embeddings + Flash)

# Vercel Blob (auto-injected on Vercel, only needed for local dev with Vercel Blob)
# BLOB_READ_WRITE_TOKEN=

# Inngest (auto-injected on Vercel via Marketplace)
# INNGEST_EVENT_KEY=
# INNGEST_SIGNING_KEY=
